import React from "react";
import Slider from "@/components/slider-component/Slider";

const PublicationsPage = ({publicationData}) => {
  return <Slider projectDetail={publicationData} />
};

export default PublicationsPage;
