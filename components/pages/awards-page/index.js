import React from "react";
import Slider from "@/components/slider-component/Slider";

const AwardsPage = ({ awardData }) => {
  return <Slider projectDetail={awardData} />
};

export default AwardsPage;
